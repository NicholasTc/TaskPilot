Idea:
Make it like notion, but for each individual day/study blocks, we use a kanban style 

April 19:
What is done so far: everything is done (basic feature), but calendar is not yet done, essentially CRUD mvp is done
To do next: make the UI pretty like in the design proposal (need to use opus for this)
After that: deploy the site to render
After render: discuss waht new feature to build